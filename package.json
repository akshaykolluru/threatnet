{
  "name": "crime-investigation-prototype",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"uvicorn api.main:app --reload --app-dir backend\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace web",
    "test": "npm run test --workspace web && pytest -q backend/tests"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
